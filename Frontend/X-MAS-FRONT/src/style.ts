import styled from "styled-components";

const AppWrapper = styled.div`
  max-width: 1200px;
  min-height: 100vh;
  margin: 0 auto;
  padding: 0 20px;
  position: relative;
`;

export default AppWrapper;
